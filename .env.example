# Dipakai oleh docker-compose untuk build args (Vite inline saat build).
# Salin ke `.env` lalu isi dari Supabase: Settings -> API.
# anon key aman untuk frontend (dilindungi RLS). Jangan pakai service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
