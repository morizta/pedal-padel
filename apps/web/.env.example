# Salin file ini ke `.env` lalu isi dari Supabase: Settings → API.
# anon key aman untuk frontend (dilindungi Row Level Security). Jangan pakai service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
